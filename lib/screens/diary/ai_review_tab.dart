import 'package:flutter/material.dart';

class AiReviewTab extends StatelessWidget {
  const AiReviewTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "준비중입니다..",
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
